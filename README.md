# vuagavuacho.cpp
#include &lt;iostream> using namespace std;  int main() {     long long n, m;     cin >> n >> m;     long long y = (m - 2 * n) / 2;     long long x = n - y;     cout &lt;&lt; x &lt;&lt; " " &lt;&lt; y; }
